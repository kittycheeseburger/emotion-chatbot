services:
  - type: web
    name: emotion-chatbot
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: APP_ENV
        value: production
      - key: CHAT_REPLY_MODE
        value: rag
